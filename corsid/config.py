import os

# Root directory of project (directory that contains this file).  Useful as the starting point for
# file I/O operations at the project level
ROOT_DIR = os.path.dirname(os.path.abspath(__file__))
